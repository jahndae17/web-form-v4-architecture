* All commands should have n dimensional tables where possible
* All commands should support linting
* All commands should have an info box that tells users their purpose, parameters & time complexity